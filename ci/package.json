{
  "name": "ci",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scripts": "1.1.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "test:cover":"react-scripts test --env=jsdom --coverage | coveralls",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "coveralls": "^3.0.2"
  },
  "jest": {
    "coverageReporters": [
      "text-lcov"
    ]
  }
}
